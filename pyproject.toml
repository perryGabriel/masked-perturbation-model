[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mpmgame"
version = "0.1.0"
description = "Masked-perturbation model security games for feedback systems"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "masked-perturbation-model contributors"}]
dependencies = [
  "numpy>=1.24",
  "scipy>=1.10",
  "control>=0.9.4",
  "matplotlib>=3.7",
  "networkx>=3.0",
  "pandas>=2.0",
  "tqdm>=4.66",
]

[project.optional-dependencies]
andes = [
  "andes>=1.9",
]
demos = [
  "jupyter>=1.0",
  "notebook>=7.0",
  "matplotlib>=3.7",
]
dev = [
  "pytest>=7.0",
  "jupyter>=1.0",
  "notebook>=7.0",
]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.package-data]
"masked_perturbation_model.cases.andes.data" = ["*.json"]
"mpmgame.cases.andes.data" = ["*.json"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
